# Parrot benchmark shop

A small pricing library. `src/price_calculator.py` turns a basket of line items
into a gross total; `src/inventory.py` holds the stock levels.

```bash
python -m pytest tests/
```
