# Per-app values for common/drive-scenario.sh. Measured against a live window;
# see MEASUREMENTS.md.
CLASS=xterm          # WM_CLASS(STRING) = "xterm", "XTerm"
WANT_W=1440          # forced by the harness; xterm alone would pick 1435
WANT_H=900           # ditto 897 - see MEASUREMENTS.md
LOAD_WAIT=6          # no toolkit, no session bus - first frame is immediate
