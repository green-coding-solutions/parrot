# Per-app values for common/drive-scenario.sh.
# WM_CLASS(STRING) = "Alacritty", "Alacritty" - capital A in BOTH fields, unlike
# every other entrant. fluxbox's match is case-sensitive so the pin string must
# keep the capital; xdotool's is not, so CLASS here is forgiving.
CLASS=Alacritty
WANT_W=1440
WANT_H=900
LOAD_WAIT=8
